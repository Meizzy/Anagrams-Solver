<resources>
    <!-- Base application theme. -->
    <style name="Theme.AnagramSolver" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/md_grey_900</item>
        <item name="colorPrimaryVariant">@color/md_grey_900</item>
        <item name="colorOnPrimary">@color/md_grey_300</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowExitTransition">@transition/transition</item>
        <item name="android:windowBackground">@color/md_grey_900</item>
    </style>

    <style name="card_view_style">
        <item name="cardBackgroundColor">@color/md_grey_800</item>
    </style>

    <style name="Theme.AnagramSolver.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>